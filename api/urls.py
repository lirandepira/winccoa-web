# api/urls.py
from django.conf.urls import url
from django.urls import path
from api import views

urlpatterns = [
    # path('api/search/'. views.search),
    # path('api/test/', views.test),
    # path('api/api_search/', views.api_search),
]